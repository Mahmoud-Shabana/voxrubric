# Nora → VoxRubric Integration Fixture

This fixture mirrors the public payload shape returned by Nora Interviewer's `export_voxrubric` path.

It intentionally exercises:

- anchor/adaptive question lanes;
- valid follow-up lineage;
- candidate thinking-time/resume controls;
- evidence graph provenance;
- practical-tool lifecycle integrity;
- tamper-evident audit-chain verification;
- bounded correlation identifiers.

The fixture is synthetic and contains no real applicant data.
